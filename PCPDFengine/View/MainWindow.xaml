<Window x:Class="PCPDFengine.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        xmlns:local="clr-namespace:PCPDFengine"
        mc:Ignorable="d"
        xmlns:Views="clr-namespace:PCPDFengine.View"
        xmlns:ViewModels="clr-namespace:PCPDFengine.ViewModel"
        MinHeight="250"
        MinWidth="500"
        ResizeMode="CanResizeWithGrip"
        Title="DataReporter">

    <Window.DataContext>
        <ViewModels:MainWindowViewModel />
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type ViewModels:MainViewModel}">
            <Views:MainView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ContentControl DockPanel.Dock="Bottom" x:Name="Pages" Content="{Binding SelectedViewModel}" />
    </Grid>
</Window>